Be-well/
├── .github/
│   └── workflows/
│       └── contract_security_audit.yml  <- أتمتة الفحص الأمني والأمان (CI/CD)
├── pi-contracts/
│   ├── src/
│   │   ├── roles.rs                     <- التحقق من الهويات (Individual, Company, ServiceProvider)
│   │   ├── insurance.rs                 <- تحصيل الأقساط والتحوط لـ AMM (Pi/YER)
│   │   └── audit.rs                     <- عقود حوكمة الـ DAO ومراقبة تضارب المصالح
│   └── Cargo.toml
├── ai-backend/
│   ├── risk_engine.py                   <- نموذج PyTorch للتسعير الديناميكي والاكتتاب
│   ├── audit_engine.py                  <- تسوية مستحقات المدققين لحظياً بالـ YER
│   ├── continuous_learning.py           <- التعلم النشط وقفل الأمان للـ AI
│   └── sandbox_attacker_simulator.py    <- محاكي الهجمات البرمجية المعزول لـ Soroban
├── backend/
│   ├── clearing_api.py                  <- المقاصة اللحظية عبر الـ Swap لـ DEX Pi
│   └── UnifiedIdentityRegistry.js       <- سجل الهوية الموحد ومنع الصرف المزدوج
└── frontend/
    ├── index.html                       <- واجهة المستخدم الرسومية الموحدة
    ├── app.js                           <- لوحة التحكم المدمجة وواجهة الطبيب الرقمي
    └── offline-sync.js                  <- محرك الشمول والتشغيل دون اتصال (Offline-first)
